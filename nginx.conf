upstream app-backend {
    server app-player:8080;
}

server {
    listen 80;

    location / {
        proxy_pass http://app-backend;
        proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }
}